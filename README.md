# io.github.ostapkonst.HashVerifier

[HashVerifier](https://github.com/ostapkonst/HashVerifier) is a cross-platform utility for generating and verifying checksum files, featuring both a GTK3 graphical and command-line interfaces. It supports multiple hash algorithms, recursive directory processing, and real-time progress tracking with speed display.
